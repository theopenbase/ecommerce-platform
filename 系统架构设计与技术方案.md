# 系统架构设计与技术方案

**货架式全类目电商平台**

| 字段 | 内容 |
|------|------|
| 文档版本 | V1.0 |
| 编写人 | Charlie（后端工程师）|
| 日期 | 2026-05-02 |
| 状态 | 完成 |
| 依赖 | PRD V1.0（Bob） |

---

## 1. 系统架构总览

### 1.1 分层架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                          客户端层                                    │
│         Web (SSR/H5)  /  iOS  /  Android  /  微信小程序             │
└─────────────────────────────────────────────────────────────────────┘
                                  │ HTTPS / HTTP2
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       API 网关层（流量入口）                          │
│         Kong / APISIX   TLS Termination │ 路由 │ 鉴权 │ 限流 │ 监控  │
└─────────────────────────────────────────────────────────────────────┘
                                  │
              ┌───────────────────┼───────────────────┐
              ▼                   ▼                   ▼
┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│   BFF 层          │  │   BFF 层          │  │   BFF 层          │
│  (C端聚合服务)    │  │  (商家端服务)     │  │  (运营后台服务)   │
│  Go + gRPC       │  │  Go + gRPC       │  │  Go + gRPC       │
└──────────────────┘  └──────────────────┘  └──────────────────┘
              │                   │                   │
              └───────────────────┼───────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        业务服务层（Domain Services）                  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │商品服务  │ │店铺服务  │ │订单服务  │ │支付服务  │ │用户服务  │ │
│  │(Product) │ │(Shop)    │ │(Order)   │ │(Payment) │ │(User)    │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘ │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │营销服务  │ │会员服务  │ │物流服务  │ │售后/维权 │ │搜索服务  │ │
│  │(Marketing)│ │(Member) │ │(Logistics)│ │(Aftersale)│ │(Search) │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘ │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐                           │
│  │结算服务  │ │库存服务  │ │通知服务  │                           │
│  │(Settlement)│ │(Stock)  │ │(Notify)  │                           │
│  └──────────┘ └──────────┘ └──────────┘                           │
└─────────────────────────────────────────────────────────────────────┘
                                  │
              ┌───────────────────┼───────────────────┐
              ▼                   ▼                   ▼
┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│    缓存层          │  │    消息队列层      │  │    搜索引擎层     │
│  Redis Cluster    │  │  RocketMQ/Kafka   │  │  Elasticsearch   │
│  (热数据/会话)     │  │  (异步解耦/事件)   │  │  (商品全量索引)   │
└──────────────────┘  └──────────────────┘  └──────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         数据存储层                                    │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐ │
│  │   MySQL          │  │   TiDB           │  │  ShardingSphere  │ │
│  │  (主库+读从库)    │  │  (分库分表)       │  │   读写分离/分片   │ │
│  │  交易/商品/店铺   │  │  订单/商品海量数据│  │                  │ │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘ │
│  ┌──────────────────┐  ┌──────────────────┐                       │
│  │   ClickHouse     │  │   MongoDB        │                       │
│  │  (OLAP分析/报表)  │  │  (商品描述/评价)  │                       │
│  └──────────────────┘  └──────────────────┘                       │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       基础设施层                                      │
│   Kubernetes (容器编排)  │  Prometheus + Grafana (监控)  │  ELK (日志)  │
│   ArgoCD (GitOps)       │  SkyWalking / Jaeger (链路追踪)              │
└─────────────────────────────────────────────────────────────────────┘
```

### 1.2 技术栈总览

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| API Gateway | Kong / APISIX | 开源高性能网关，支持插件扩展 |
| BFF | Go | 高性能 gRPC/HTTP2 聚合服务 |
| 业务服务 | Go / Python | Go 处理高并发核心链路；Python 处理营销/报表等复杂业务逻辑 |
| 数据库 | MySQL 8.0 + TiDB | MySQL 事务型数据；TiDB 海量订单/商品分库分表 |
| 缓存 | Redis Cluster 7.x | 热数据、会话、分布式锁、消息队列代理持久化 |
| 消息队列 | RocketMQ 5.x | 异步解耦、事件驱动、分布式事务最终一致性 |
| 搜索引擎 | Elasticsearch 8.x | 商品全文检索、搜索排序、关键词广告 |
| OLAP | ClickHouse | 运营大屏、实时分析、用户行为分析 |
| 对象存储 | 阿里云 OSS | 商品图片/视频/CDN 回源 |
| 容器 | Kubernetes | 服务编排、弹性伸缩、灰度发布 |
| 配置中心 | Nacos | 服务注册发现、配置管理 |
| 链路追踪 | SkyWalking 9.x | 全链路追踪、性能剖析 |
| 日志 | ELK Stack | 日志采集、搜索分析 |

### 1.3 微服务拆分策略

按业务领域（Domain）垂直拆分，按读写分离水平扩展：

| 服务 | 技术 | 职责边界 | 扩展策略 |
|------|------|---------|---------|
| product-svc | Go | SPU/SKU/类目/属性/品牌/审核 | 读从库 + Redis缓存 |
| shop-svc | Go | 店铺入驻/信息/装修/绩效 | 读从库 |
| order-svc | Go + Python | 订单/子订单/逆向流程/超时关单 | TiDB 分库分表 |
| payment-svc | Go | 支付路由/担保交易/账务/退款 | 独立部署，事务优先 |
| user-svc | Go | 用户/会员/积分/收货地址/安全 | 读从库 |
| marketing-svc | Python | 促销/优惠券/活动/秒杀/拼团 | 读写分离 + Redis 热点抢购 |
| member-svc | Go | 会员等级/成长值/权益 | 定时任务 + Redis 缓存 |
| logistics-svc | Go | 运费模板/电子面单/轨迹 | 轻量服务 |
| aftersale-svc | Go | 退款/退货/换货/仲裁 | 事件驱动 |
| search-svc | Go + ES | 商品搜索/热词/广告竞价 | ES 独立集群 |
| stock-svc | Go | 库存预占/释放/超卖防控 | Redis 分布式锁 |
| settlement-svc | Python | 商家结算/账单/提现 | 定时批处理 |
| notify-svc | Go | 短信/推送/站内消息/邮件 | 异步队列 |
| file-svc | Go | 图片上传/处理/压缩/格式转换 | 阿里云 OSS |

---

## 2. 数据库设计

### 2.1 数据库整体规划

| 库名 | 引擎 | 用途 | 扩展方式 |
|------|------|------|---------|
| ecommerce_product | MySQL InnoDB | SPU/SKU/类目/属性/品牌 | 读从库 |
| ecommerce_shop | MySQL InnoDB | 店铺/资质/绩效 | 读从库 |
| ecommerce_user | MySQL InnoDB | 用户/会员/积分/地址 | 读从库 |
| ecommerce_order | TiDB | 订单/子订单/订单项 | 分库分表（按买家ID） |
| ecommerce_payment | TiDB | 支付流水/账务/退款 | 分库分表（按支付渠道） |
| ecommerce_marketing | MySQL InnoDB | 促销/优惠券/活动 | 读从库 |
| ecommerce_aftersale | MySQL InnoDB | 售后/维权/仲裁 | 读从库 |
| ecommerce_settlement | MySQL InnoDB | 商家账单/结算/提现 | 读从库 |
| goods_elasticsearch | ES Index | 商品全文检索索引 | ES 分片副本 |
| analytics_clickhouse | ClickHouse | 行为日志/报表/大屏 | 分区表 |

### 2.2 核心表结构设计

#### 2.2.1 商品模块（product 库）

```sql
-- SPU 表（跨 SKU 共享的共性信息）
CREATE TABLE spu (
    id            BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT COMMENT 'SPU ID',
    spu_code      VARCHAR(32) NOT NULL UNIQUE COMMENT 'SPU 编码',
    title         VARCHAR(80) NOT NULL COMMENT '商品标题',
    short_desc    VARCHAR(200) NOT NULL COMMENT '短描述',
    brand_id      BIGINT UNSIGNED NOT NULL COMMENT '品牌 ID',
    category_id   BIGINT UNSIGNED NOT NULL COMMENT '三级类目 ID',
    unit          VARCHAR(16) NOT NULL COMMENT '单位',
    origin        VARCHAR(64) DEFAULT '' COMMENT '产地',
    status        TINYINT NOT NULL DEFAULT 0 COMMENT '0-待上架 1-上架 2-下架 3-归档',
    shop_id       BIGINT UNSIGNED NOT NULL COMMENT '店铺 ID',
    audit_status  TINYINT NOT NULL DEFAULT 0 COMMENT '0-待审核 1-通过 2-拒绝',
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_category (category_id),
    INDEX idx_brand (brand_id),
    INDEX idx_shop (shop_id),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='SPU 表';

-- SKU 表（具体规格售卖单元）
CREATE TABLE sku (
    id            BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT COMMENT 'SKU ID',
    sku_code      VARCHAR(64) NOT NULL UNSIGNED COMMENT 'SKU 编码（商家自定义，平台唯一）',
    spu_id        BIGINT UNSIGNED NOT NULL COMMENT '所属 SPU ID',
    shop_id       BIGINT UNSIGNED NOT NULL COMMENT '店铺 ID',
    price_tag     DECIMAL(12,2) NOT NULL COMMENT '吊牌价（划线价）',
    price_sell    DECIMAL(12,2) NOT NULL COMMENT '销售价',
    price_cost    DECIMAL(12,2) DEFAULT NULL COMMENT '成本价（商家可见）',
    stock         INT NOT NULL DEFAULT 0 COMMENT '库存数量',
    stock_warn    INT DEFAULT 0 COMMENT '库存预警值',
    freight_id    BIGINT UNSIGNED NOT NULL COMMENT '运费模板 ID',
    delivery_region VARCHAR(512) NOT NULL COMMENT '发货地（省市区）',
    delivery_time TINYINT NOT NULL COMMENT '发货时间（小时）',
    status        TINYINT NOT NULL DEFAULT 0 COMMENT '0-待上架 1-上架 2-下架 3-售罄 4-归档',
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_sku_code (sku_code),
    INDEX idx_spu (spu_id),
    INDEX idx_shop (shop_id),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='SKU 表';

-- SKU 属性值表（规格组合）
CREATE TABLE sku_attr (
    id       BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    sku_id   BIGINT UNSIGNED NOT NULL COMMENT 'SKU ID',
    attr_name VARCHAR(32) NOT NULL COMMENT '属性名，如颜色/尺码',
    attr_value VARCHAR(128) NOT NULL COMMENT '属性值，如红色/L',
    INDEX idx_sku (sku_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='SKU 属性表';

-- SKU 图片表
CREATE TABLE sku_image (
    id      BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    sku_id  BIGINT UNSIGNED NOT NULL,
    url     VARCHAR(512) NOT NULL COMMENT '图片 OSS URL',
    is_main TINYINT NOT NULL DEFAULT 0 COMMENT '是否主图',
    sort    TINYINT NOT NULL DEFAULT 0 COMMENT '排序',
    INDEX idx_sku (sku_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='SKU 图片表';

-- 类目表（邻接表模型，支持无限层级，建议≤4 级）
CREATE TABLE category (
    id         BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    name       VARCHAR(32) NOT NULL COMMENT '类目名称',
    parent_id  BIGINT UNSIGNED NOT NULL DEFAULT 0 COMMENT '父类目 ID，0 为根',
    level      TINYINT NOT NULL COMMENT '层级 1/2/3/4',
    path       VARCHAR(128) NOT NULL DEFAULT '' COMMENT '类目路径，如 /1/3/5/',
    sort       INT NOT NULL DEFAULT 0 COMMENT '排序',
    status     TINYINT NOT NULL DEFAULT 1 COMMENT '1-正常 0-禁用',
    is_leaf    TINYINT NOT NULL DEFAULT 0 COMMENT '是否叶子类目',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_parent (parent_id),
    INDEX idx_path (path)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='类目表';

-- 类目属性模板表
CREATE TABLE category_attr_template (
    id            BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    category_id   BIGINT UNSIGNED NOT NULL COMMENT '末级类目 ID',
    attr_name     VARCHAR(32) NOT NULL COMMENT '属性名',
    attr_type     VARCHAR(16) NOT NULL COMMENT '类型：text/number/boolean/multi',
    is_required   TINYINT NOT NULL DEFAULT 0 COMMENT '是否必填',
    sort          INT NOT NULL DEFAULT 0,
    INDEX idx_category (category_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='类目属性模板';

-- 品牌表
CREATE TABLE brand (
    id         BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    name       VARCHAR(64) NOT NULL UNIQUE COMMENT '品牌名称',
    logo_url   VARCHAR(512) DEFAULT '' COMMENT '品牌 Logo',
    status     TINYINT NOT NULL DEFAULT 1 COMMENT '1-正常 0-禁用',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='品牌表';
```

#### 2.2.2 店铺模块（shop 库）

```sql
-- 店铺表
CREATE TABLE shop (
    id            BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    shop_code     VARCHAR(32) NOT NULL UNIQUE COMMENT '店铺编码',
    name          VARCHAR(64) NOT NULL COMMENT '店铺名称',
    type          TINYINT NOT NULL COMMENT '1-自营 2-旗舰 3-专卖 4-专营',
    owner_id      BIGINT UNSIGNED NOT NULL COMMENT '店主用户 ID',
    status        TINYINT NOT NULL DEFAULT 0 COMMENT '0-待审核 1-正常 2-封禁 3-清退',
    logo_url      VARCHAR(512) DEFAULT '' COMMENT '店铺 Logo',
    banner_url    VARCHAR(512) DEFAULT '' COMMENT '店招 Banner',
    description   VARCHAR(500) DEFAULT '' COMMENT '店铺简介',
    province      VARCHAR(32) DEFAULT '' COMMENT '省份',
    city          VARCHAR(32) DEFAULT '' COMMENT '城市',
    credit_score  DECIMAL(3,2) DEFAULT 0 COMMENT '综合评分',
    dsr_product   DECIMAL(3,2) DEFAULT 0 COMMENT '商品描述 DSR',
    dsr_service   DECIMAL(3,2) DEFAULT 0 COMMENT '服务态度 DSR',
    dsr_logistics DECIMAL(3,2) DEFAULT 0 COMMENT '物流速度 DSR',
    follower_count INT UNSIGNED DEFAULT 0 COMMENT '关注数',
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_owner (owner_id),
    INDEX idx_type (type),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='店铺表';

-- 店铺入驻资质表
CREATE TABLE shop_qualification (
    id         BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    shop_id    BIGINT UNSIGNED NOT NULL,
    qual_type  VARCHAR(32) NOT NULL COMMENT '资质类型：business_license/tm_card/food_license...',
    cert_no    VARCHAR(64) DEFAULT '' COMMENT '证书编号',
    front_url  VARCHAR(512) NOT NULL COMMENT '正面图片 URL',
    back_url   VARCHAR(512) DEFAULT '' COMMENT '背面图片 URL（身份证用）',
    expiry_date DATE DEFAULT NULL COMMENT '有效期',
    status     TINYINT NOT NULL DEFAULT 0 COMMENT '0-待审 1-通过 2-拒绝',
    audited_at DATETIME DEFAULT NULL,
    auditor_id BIGINT UNSIGNED DEFAULT NULL,
    INDEX idx_shop (shop_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='店铺资质表';

-- 店铺保证金表
CREATE TABLE shop_deposit (
    id           BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    shop_id      BIGINT UNSIGNED NOT NULL,
    amount       DECIMAL(12,2) NOT NULL COMMENT '保证金金额',
    status       TINYINT NOT NULL DEFAULT 0 COMMENT '0-冻结 1-可用 2-扣除中',
    freeze_time  DATETIME DEFAULT NULL,
    unfreeze_time DATETIME DEFAULT NULL,
    INDEX idx_shop (shop_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='店铺保证金';

-- 运费模板表
CREATE TABLE freight_template (
    id          BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    shop_id     BIGINT UNSIGNED NOT NULL,
    name        VARCHAR(32) NOT NULL COMMENT '模板名称',
    type        TINYINT NOT NULL COMMENT '1-按件 2-按重量 3-固定 4-地区计价',
    is_free_threshold TINYINT NOT NULL DEFAULT 0 COMMENT '是否满足条件包邮',
    free_amount DECIMAL(12,2) DEFAULT 0 COMMENT '满 X 元包邮',
    free_num    INT DEFAULT 0 COMMENT '满 X 件包邮',
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_shop (shop_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='运费模板';

-- 运费规则表（地区维度）
CREATE TABLE freight_rule (
    id              BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    template_id     BIGINT UNSIGNED NOT NULL,
    province_codes  VARCHAR(256) NOT NULL COMMENT '省市区代码，逗号分隔',
    first_amount    DECIMAL(10,2) NOT NULL COMMENT '首费（首件/首重）',
    first_num       INT NOT NULL DEFAULT 1 COMMENT '首件数量/首重 kg',
    add_amount      DECIMAL(10,2) NOT NULL COMMENT '续费',
    add_num         INT NOT NULL DEFAULT 1 COMMENT '续件数量/续重 kg',
    INDEX idx_template (template_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='运费规则';
```

#### 2.2.3 订单模块（order 库，TiDB 分库分表）

```sql
-- 父订单表（按买家 ID 分片）
CREATE TABLE parent_order (
    id              BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT COMMENT '订单 ID',
    order_no        VARCHAR(32) NOT NULL UNIQUE COMMENT '订单编号',
    buyer_id        BIGINT UNSIGNED NOT NULL COMMENT '买家用户 ID',
    total_amount    DECIMAL(12,2) NOT NULL COMMENT '商品总额',
    freight_amount  DECIMAL(12,2) NOT NULL DEFAULT 0 COMMENT '运费总额',
    discount_amount DECIMAL(12,2) NOT NULL DEFAULT 0 COMMENT '优惠总额',
    pay_amount      DECIMAL(12,2) NOT NULL COMMENT '实付金额',
    status          TINYINT NOT NULL COMMENT '0-待付款 1-已取消 2-待发货 3-待收货 4-已收货 5-已完成 6-维权中',
    pay_time        DATETIME DEFAULT NULL COMMENT '付款时间',
    delivery_time   DATETIME DEFAULT NULL COMMENT '发货时间',
    receive_time    DATETIME DEFAULT NULL COMMENT '收货时间',
    finish_time     DATETIME DEFAULT NULL COMMENT '完成时间',
    cancel_time     DATETIME DEFAULT NULL COMMENT '取消时间',
    cancel_reason   VARCHAR(256) DEFAULT '' COMMENT '取消原因',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_buyer (buyer_id),
    INDEX idx_status (status),
    INDEX idx_pay_time (pay_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='父订单';

-- 子订单表（按买家 ID 分片）
CREATE TABLE sub_order (
    id              BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    sub_order_no    VARCHAR(32) NOT NULL UNIQUE COMMENT '子订单编号',
    parent_order_no VARCHAR(32) NOT NULL COMMENT '父订单编号',
    buyer_id        BIGINT UNSIGNED NOT NULL COMMENT '买家 ID',
    shop_id         BIGINT UNSIGNED NOT NULL COMMENT '店铺 ID',
    status          TINYINT NOT NULL COMMENT '同父订单状态语义',
    freight_amount  DECIMAL(12,2) NOT NULL DEFAULT 0 COMMENT '子订单运费',
    discount_amount DECIMAL(12,2) NOT NULL DEFAULT 0 COMMENT '子订单优惠',
    pay_amount      DECIMAL(12,2) NOT NULL COMMENT '子订单实付',
    invoice_type    TINYINT DEFAULT 0 COMMENT '0-不开发票 1-普票 2-增票',
    invoice_title   VARCHAR(128) DEFAULT '' COMMENT '发票抬头',
    invoice_tax_no  VARCHAR(32) DEFAULT '' COMMENT '税号',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_parent (parent_order_no),
    INDEX idx_buyer (buyer_id),
    INDEX idx_shop (shop_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='子订单';

-- 订单商品项表（按子订单号分片 or 随子订单同分片）
CREATE TABLE order_item (
    id              BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    sub_order_no    VARCHAR(32) NOT NULL COMMENT '子订单编号',
    sku_id          BIGINT UNSIGNED NOT NULL COMMENT 'SKU ID',
    spu_id          BIGINT UNSIGNED NOT NULL COMMENT 'SPU ID',
    sku_code        VARCHAR(64) NOT NULL COMMENT '下单时 SKU 编码快照',
    title           VARCHAR(80) NOT NULL COMMENT '商品标题快照',
    sku_attrs       VARCHAR(512) NOT NULL COMMENT 'SKU 属性快照 JSON',
    price_tag       DECIMAL(12,2) NOT NULL COMMENT '吊牌价快照',
    price_sell      DECIMAL(12,2) NOT NULL COMMENT '销售价快照',
    quantity        INT NOT NULL COMMENT '购买数量',
    discount_amount DECIMAL(12,2) NOT NULL DEFAULT 0 COMMENT '商品级优惠分摊',
    item_total      DECIMAL(12,2) NOT NULL COMMENT '小计',
    refund_status   TINYINT NOT NULL DEFAULT 0 COMMENT '0-无退款 1-部分退款 2-全额退款',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_sub_order (sub_order_no),
    INDEX idx_sku (sku_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单商品项';

-- 收货地址表
CREATE TABLE receiver_address (
    id           BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    user_id      BIGINT UNSIGNED NOT NULL,
    receiver     VARCHAR(32) NOT NULL COMMENT '收货人',
    mobile       VARCHAR(20) NOT NULL COMMENT '手机号',
    province_code VARCHAR(10) NOT NULL COMMENT '省代码',
    city_code     VARCHAR(10) NOT NULL COMMENT '市代码',
    district_code VARCHAR(10) NOT NULL COMMENT '区代码',
    province_name VARCHAR(32) NOT NULL,
    city_name     VARCHAR(32) NOT NULL,
    district_name VARCHAR(32) NOT NULL,
    detail        VARCHAR(256) NOT NULL COMMENT '详细地址',
    tag           VARCHAR(16) DEFAULT '' COMMENT '标签：家/公司/父母',
    is_default    TINYINT NOT NULL DEFAULT 0,
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='收货地址';
```

#### 2.2.4 支付模块（payment 库）

```sql
-- 支付流水表
CREATE TABLE payment_transaction (
    id              BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    trans_no        VARCHAR(64) NOT NULL UNIQUE COMMENT '平台交易流水号',
    order_no        VARCHAR(32) NOT NULL COMMENT '关联订单号（父 or 子）',
    sub_order_no    VARCHAR(32) DEFAULT '' COMMENT '子订单号',
    buyer_id        BIGINT UNSIGNED NOT NULL,
    channel         VARCHAR(16) NOT NULL COMMENT 'alipay/wechat/bank/balance',
    channel_trans_no VARCHAR(128) DEFAULT '' COMMENT '渠道方流水号',
    amount          DECIMAL(12,2) NOT NULL COMMENT '支付金额',
    status          TINYINT NOT NULL COMMENT '0-待支付 1-支付中 2-成功 3-失败 4-已退款',
    pay_time        DATETIME DEFAULT NULL,
    expire_time     DATETIME NOT NULL COMMENT '支付过期时间',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_order (order_no),
    INDEX idx_buyer (buyer_id),
    INDEX idx_channel (channel),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='支付流水';

-- 账户表（买家/商家账户）
CREATE TABLE account (
    id           BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    user_id      BIGINT UNSIGNED NOT NULL COMMENT '用户 ID，平台唯一',
    user_type    TINYINT NOT NULL COMMENT '1-买家 2-商家 3-平台',
    balance      DECIMAL(14,2) NOT NULL DEFAULT 0 COMMENT '账户余额',
    freeze_balance DECIMAL(14,2) NOT NULL DEFAULT 0 COMMENT '冻结金额',
    password_pay VARCHAR(64) DEFAULT '' COMMENT '支付密码（加盐哈希）',
    status       TINYINT NOT NULL DEFAULT 1 COMMENT '1-正常 0-冻结',
    created_at   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user (user_id, user_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='账户表';

-- 账务流水表
CREATE TABLE account_log (
    id           BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    account_id   BIGINT UNSIGNED NOT NULL,
    trans_no     VARCHAR(64) NOT NULL COMMENT '关联支付流水号',
    order_no     VARCHAR(32) DEFAULT '',
    type         VARCHAR(16) NOT NULL COMMENT 'recharge/pay/refund/settle/freeze/unfreeze',
    amount       DECIMAL(12,2) NOT NULL COMMENT '正数入账，负数出账',
    balance_before DECIMAL(14,2) NOT NULL COMMENT '变动前余额',
    balance_after  DECIMAL(14,2) NOT NULL COMMENT '变动后余额',
    note         VARCHAR(256) DEFAULT '',
    created_at   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_account (account_id),
    INDEX idx_order (order_no)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='账务流水';
```

#### 2.2.5 用户/会员模块（user 库）

```sql
-- 用户表
CREATE TABLE `user` (
    id            BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    mobile        VARCHAR(20) NOT NULL UNIQUE COMMENT '手机号',
    nickname      VARCHAR(32) NOT NULL,
    avatar_url    VARCHAR(512) DEFAULT '',
    gender        TINYINT DEFAULT 0 COMMENT '0-未知 1-男 2-女',
    status        TINYINT NOT NULL DEFAULT 1 COMMENT '1-正常 0-封禁',
    last_login_at DATETIME DEFAULT NULL,
    last_login_ip VARCHAR(45) DEFAULT '',
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_mobile (mobile)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 会员等级表
CREATE TABLE member_level (
    id        BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    level     TINYINT NOT NULL UNIQUE COMMENT '等级 0-4',
    name      VARCHAR(16) NOT NULL COMMENT '铜牌/银牌...',
    threshold DECIMAL(12,2) NOT NULL COMMENT '累计消费门槛',
    rights    JSON COMMENT '权益列表',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='会员等级';

-- 会员账户表
CREATE TABLE member (
    id             BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    user_id        BIGINT UNSIGNED NOT NULL UNIQUE,
    level          TINYINT NOT NULL DEFAULT 0,
    total_spend    DECIMAL(14,2) NOT NULL DEFAULT 0 COMMENT '累计实付（不含退款）',
    growth_value   BIGINT NOT NULL DEFAULT 0 COMMENT '成长值',
    grace_end_date DATE DEFAULT NULL COMMENT '降级风险提示截止日',
    upgraded_at    DATETIME DEFAULT NULL COMMENT '最近升级时间',
    created_at     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_level (level),
    INDEX idx_user (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='会员账户';

-- 积分账户表
CREATE TABLE points_account (
    id          BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    user_id     BIGINT UNSIGNED NOT NULL UNIQUE,
    balance     BIGINT NOT NULL DEFAULT 0 COMMENT '积分余额',
    total_earned BIGINT NOT NULL DEFAULT 0 COMMENT '累计获取',
    total_spent BIGINT NOT NULL DEFAULT 0 COMMENT '累计消耗',
    expire_date DATE DEFAULT NULL COMMENT '即将过期日期',
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_user (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='积分账户';

-- 积分流水表
CREATE TABLE points_log (
    id          BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    user_id     BIGINT UNSIGNED NOT NULL,
    order_no    VARCHAR(32) DEFAULT '' COMMENT '关联订单',
    type        VARCHAR(16) NOT NULL COMMENT 'order/sign/eval/redeem/expire/refund',
    points      BIGINT NOT NULL COMMENT '正数获取，负数消耗',
    balance_before BIGINT NOT NULL,
    balance_after  BIGINT NOT NULL,
    expire_date DATE DEFAULT NULL COMMENT '本次积分过期日',
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user (user_id),
    INDEX idx_order (order_no)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='积分流水';
```

#### 2.2.6 营销模块（marketing 库）

```sql
-- 优惠券表
CREATE TABLE coupon (
    id            BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    coupon_code   VARCHAR(32) NOT NULL UNIQUE COMMENT '券模板编码',
    name          VARCHAR(32) NOT NULL COMMENT '优惠券名称',
    type          TINYINT NOT NULL COMMENT '1-平台券 2-店铺券 3-商品券 4-内部券',
    face_value    DECIMAL(10,2) NOT NULL COMMENT '面额',
    threshold     DECIMAL(10,2) NOT NULL DEFAULT 0 COMMENT '满减门槛，0 为无门槛',
    total_count   INT NOT NULL COMMENT '发放总量',
    remain_count  INT NOT NULL COMMENT '剩余数量',
    per_user_limit INT NOT NULL DEFAULT 1 COMMENT '每人限领张数',
    valid_type    TINYINT NOT NULL COMMENT '1-绝对时间 2-相对领取日',
    valid_start   DATETIME DEFAULT NULL,
    valid_end     DATETIME DEFAULT NULL,
    valid_days    INT DEFAULT NULL COMMENT '领取后 N 天有效',
    applicable_type TINYINT NOT NULL DEFAULT 0 COMMENT '0-全品 1-指定类目 2-指定商品 3-指定店铺',
    applicable_ids VARCHAR(512) DEFAULT '' COMMENT '关联 ID 列表，逗号分隔',
    shop_id       BIGINT UNSIGNED DEFAULT NULL COMMENT '店铺券时关联店铺',
    status        TINYINT NOT NULL DEFAULT 1 COMMENT '1-待发放 2-发放中 3-已领完 4-已过期',
    created_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_shop (shop_id),
    INDEX idx_type (type),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='优惠券';

-- 用户优惠券表
CREATE TABLE user_coupon (
    id            BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    user_id       BIGINT UNSIGNED NOT NULL,
    coupon_id     BIGINT UNSIGNED NOT NULL,
    coupon_code   VARCHAR(32) NOT NULL COMMENT '用户领取到的券实例编码',
    status        TINYINT NOT NULL DEFAULT 0 COMMENT '0-未使用 1-已使用 2-已过期 3-已退款',
    received_at   DATETIME NOT NULL COMMENT '领取时间',
    used_at       DATETIME DEFAULT NULL,
    used_order_no VARCHAR(32) DEFAULT '',
    expire_date   DATE NOT NULL COMMENT '过期日期',
    INDEX idx_user (user_id),
    INDEX idx_status (status),
    INDEX idx_expire (expire_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户优惠券';

-- 促销活动表
CREATE TABLE promotion (
    id          BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    name        VARCHAR(64) NOT NULL COMMENT '活动名称',
    type        VARCHAR(16) NOT NULL COMMENT 'full_reduce/flash_seckill/group_buy/tier_price/pre_sell',
    start_time  DATETIME NOT NULL,
    end_time    DATETIME NOT NULL,
    status      TINYINT NOT NULL DEFAULT 0 COMMENT '0-未开始 1-进行中 2-已结束',
    rules       JSON NOT NULL COMMENT '促销规则 JSON',
    created_at   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_status (status),
    INDEX idx_time (start_time, end_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='促销活动';

-- 活动商品表
CREATE TABLE promotion_sku (
    id           BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    promotion_id BIGINT UNSIGNED NOT NULL,
    sku_id       BIGINT UNSIGNED NOT NULL,
    promo_price  DECIMAL(12,2) NOT NULL COMMENT '活动价',
    stock_limit  INT
-- 活动商品表
CREATE TABLE promotion_sku (
    id           BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    promotion_id BIGINT UNSIGNED NOT NULL,
    sku_id       BIGINT UNSIGNED NOT NULL,
    promo_price  DECIMAL(12,2) NOT NULL COMMENT '活动价',
    stock_limit  INT NOT NULL COMMENT '活动库存上限',
    sold_count   INT NOT NULL DEFAULT 0 COMMENT '已售数量',
    INDEX idx_promotion (promotion_id),
    INDEX idx_sku (sku_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='活动商品';

-- 售后/维权表
CREATE TABLE aftersale (
    id            BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    aftersale_no  VARCHAR(32) NOT NULL UNIQUE COMMENT '售后单号',
    sub_order_no  VARCHAR(32) NOT NULL COMMENT '关联子订单',
    buyer_id      BIGINT UNSIGNED NOT NULL,
    shop_id       BIGINT UNSIGNED NOT NULL,
    type          TINYINT NOT NULL COMMENT '1-仅退款 2-退货退款 3-换货 4-维修',
    status        TINYINT NOT NULL COMMENT '0-待处理 1-商家响应 2-买家退货 3-商家收货 4-平台仲裁 5-完成 6-关闭',
    refund_amount DECIMAL(12,2) NOT NULL COMMENT '申请退款金额',
    refund_reason VARCHAR(256) NOT NULL COMMENT '退款原因',
    buyer_desc    TEXT COMMENT '买家描述',
    seller_reply  TEXT COMMENT '商家回复',
    platform_note TEXT COMMENT '平台仲裁备注',
    return_logistics_no VARCHAR(64) DEFAULT '' COMMENT '退货物流单号',
    created_at   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_buyer (buyer_id),
    INDEX idx_shop (shop_id),
    INDEX idx_sub_order (sub_order_no),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='售后/维权表';

-- 商家结算账单表
CREATE TABLE settlement_bill (
    id            BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    bill_no       VARCHAR(32) NOT NULL UNIQUE COMMENT '账单编号',
    shop_id       BIGINT UNSIGNED NOT NULL,
    period_start  DATE NOT NULL COMMENT '结算周期开始',
    period_end    DATE NOT NULL COMMENT '结算周期结束',
    order_amount  DECIMAL(14,2) NOT NULL COMMENT '订单金额',
    refund_amount DECIMAL(14,2) NOT NULL DEFAULT 0 COMMENT '退款金额',
    commission    DECIMAL(14,2) NOT NULL COMMENT '平台佣金',
    coupon subsidy DECIMAL(14,2) NOT NULL DEFAULT 0 COMMENT '优惠券补贴',
    settle_amount DECIMAL(14,2) NOT NULL COMMENT '结算金额',
    status        TINYINT NOT NULL DEFAULT 0 COMMENT '0-待确认 1-待打款 2-已打款 3-有异议',
    settle_time   DATETIME DEFAULT NULL COMMENT '结算时间',
    created_at   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_shop (shop_id),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商家结算账单';
```

### 2.3 分库分表策略

| 数据维度 | 分片键 | 分片算法 | 片数 |
|---------|--------|---------|------|
| 订单（order库） | buyer_id | 哈希取模 | 16/32/64 片 |
| 订单（order库） | shop_id（子查询） | 买家 ID 二次映射 | 同上 |
| 支付流水（payment库） | channel + 日期 | 渠道分库+月度分表 | 按渠道 4~8 库 |
| 商品（海量SKU） | category_id | 类别映射 + 哈希 | ES 索引解决 |
| 用户（user库） | user_id | 哈希取模 | 读从库 + Redis |

---

## 3. API 规范设计

### 3.1 RESTful API 设计原则

- 资源命名： `/api/v1/{resource}`，复数名词
- HTTP 方法： GET（查询）、POST（创建）、PUT（全量更新）、PATCH（增量更新）、DELETE（删除）
- 状态码： 200 OK、201 Created、400 Bad Request、401 Unauthorized、403 Forbidden、404 Not Found、409 Conflict、422 Unprocessable Entity、500 Internal Server Error
- 统一响应结构：

```json
{
  "code": 0,
  "message": "success",
  "data": { ... },
  "request_id": "uuid-v4",
  "timestamp": 1714622400000
}
```

错误码规范（code）：

| 区间 | 含义 |
|------|------|
| 0 | 成功 |
| 1xxx | 通用错误（参数校验、权限等） |
| 2xxx | 商品模块错误 |
| 3xxx | 订单模块错误 |
| 4xxx | 支付模块错误 |
| 5xxx | 用户/会员模块错误 |
| 6xxx | 营销模块错误 |
| 7xxx | 店铺模块错误 |
| 9xxx | 系统级错误 |

### 3.2 核心 API 清单（C端）

#### 商品模块

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/v1/products/spu/{spuId} | 获取 SPU 详情（含 SKU 列表） |
| GET | /api/v1/products/sku/{skuId} | 获取 SKU 详情 |
| GET | /api/v1/products | 分页查询 SPU 列表（支持类目/品牌/价格筛选） |
| GET | /api/v1/categories | 获取类目树 |
| GET | /api/v1/categories/{id}/attributes | 获取类目属性模板 |

#### 订单模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/orders | 创建父订单（含子订单拆单） |
| GET | /api/v1/orders | 查询父订单列表（分页） |
| GET | /api/v1/orders/{orderNo} | 获取父订单详情（含子订单） |
| GET | /api/v1/orders/{orderNo}/sub | 获取子订单列表 |
| PUT | /api/v1/orders/{orderNo}/cancel | 取消订单（待付款） |
| PUT | /api/v1/orders/{orderNo}/receive | 确认收货 |
| POST | /api/v1/orders/{orderNo}/aftersale | 发起售后 |

#### 支付模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/payments/pay | 发起支付（返回唤起支付参数） |
| POST | /api/v1/payments/callback/{channel} | 支付回调（支付宝/微信） |
| GET | /api/v1/payments/{transNo} | 查询支付状态 |
| POST | /api/v1/payments/refund | 申请退款 |

#### 用户/会员模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/v1/users/register | 用户注册 |
| POST | /api/v1/users/login | 登录（手机+验证码/密码） |
| GET | /api/v1/users/me | 获取当前用户信息 |
| GET | /api/v1/users/addresses | 获取收货地址列表 |
| POST | /api/v1/users/addresses | 新增收货地址 |
| GET | /api/v1/members/profile | 获取会员信息（含等级权益） |
| GET | /api/v1/points/balance | 查询积分余额 |
| GET | /api/v1/coupons/mine | 获取我的优惠券 |

### 3.3 服务间 gRPC 接口（内部）

```protobuf
// 订单服务 → 商品服务：库存扣减
rpc StockDeduct (StockDeductRequest) returns (StockDeductResponse);
rpc StockRollback (StockRollbackRequest) returns (StockRollbackResponse);

// 订单服务 → 支付服务：冻结款
rpc FreezeAccount (FreezeAccountRequest) returns (FreezeAccountResponse);

// 支付服务 → 订单服务：支付成功回调
rpc ConfirmPayment (ConfirmPaymentRequest) returns (ConfirmPaymentResponse);

// 订单服务 → 营销服务：优惠券核销
rpc RedeemCoupon (RedeemCouponRequest) returns (RedeemCouponResponse);

// 订单服务 → 会员服务：积分发放
rpc GrantPoints (GrantPointsRequest) returns (GrantPointsResponse);

// 订单服务 → 物流服务：获取运费
rpc CalculateFreight (CalculateFreightRequest) returns (CalculateFreightResponse);
```

### 3.4 API 版本与向后兼容策略

- URL 路径版本化：`/api/v1/` → `/api/v2/`
- 字段级别的向前兼容（Add-only）：新增字段不破坏旧客户端
- 枚举类字段：客户端按 string 处理，服务端兼容新旧值
- 废弃策略：字段 `deprecated=true`，2个大版本后删除

---

## 4. 安全设计

### 4.1 认证与授权

| 机制 | 实现方式 |
|------|---------|
| 身份认证 | JWT AccessToken（15min）+ RefreshToken（7d），存 Redis 黑名单 |
| 敏感操作 | 支付密码 + 短信验证码二次确认 |
| 管理员认证 | JWT + RBAC 权限模型（角色：超级管理员/运营/客服/财务/风控/审计） |
| 商家认证 | JWT + 店铺 ID 绑定，接口层校验 shop_id 归属 |
| 接口鉴权 | 网关层统一校验 Token；BFF 层校验业务权限；服务层校验数据归属 |
| CSRF | 网关校验 Origin/Referer；敏感 Cookie 加 SameSite |

### 4.2 数据安全

| 层面 | 措施 |
|------|------|
| 传输层 | 全站 HTTPS + TLS 1.2+；HTTP Strict Transport Security |
| 密码存储 | 用户密码：bcrypt cost=12；支付密码：独立 salt + bcrypt |
| 敏感字段 | 手机号/身份证：AES-256-GCM 加密存储，解密需审批 |
| 数据库 | 测试环境数据脱敏；生产数据访问需数据安全审批 |
| 日志 | 脱敏后入日志库，敏感字段打码（手机号 138****1234） |
| 买家隐私 | 商家不可查询买家手机号，通过平台消息通道中转 |

### 4.3 交易安全

| 机制 | 说明 |
|------|------|
| 限流 | 网关层：每 IP/UID 限流；业务层：关键接口（下单/支付）单机 500QPS |
| 防刷 | 下单前行为验证码（埋点/滑块）；同 UID 下单频率监控 |
| 风控 | 支付环节：金额阈值 + 异地 + 短时频繁触发风控，人工/自动处理 |
| 幂等 | 所有写接口带业务幂等键（orderIdempotentKey），防重复提交 |
| 对账 | 日终：平台侧支付流水 vs 渠道方账单自动核对，差异自动告警 |

### 4.4 审计日志

```json
{
  "event_id": "uuid",
  "operator_id": "user_123",
  "operator_type": "backend_user",
  "action": "order.refund.approve",
  "resource_type": "aftersale",
  "resource_id": "afs_456",
  "before": { "status": "pending" },
  "after": { "status": "refunded" },
  "ip": "10.0.0.1",
  "user_agent": "...",
  "timestamp": "2026-05-02T10:30:00+08:00"
}
```

- 所有写操作（增/删/改）记录审计日志
- 审计日志保留 ≥ 3 年
- 写入采用异步队列，不影响主链路

---

## 5. 关键技术方案

### 5.1 分布式事务方案：Seata AT 模式 + Saga 补偿

**选型理由**：电商场景以"柔性事务 + 最终一致性"为主，仅在支付环节需要强一致性保障。

#### 5.1.1 强一致性场景（Seata AT）

适用：支付回调、商家结算

```
用户支付成功
  → Seata TC 开启全局事务
    → order-svc：更新订单状态（AT 模式自动回滚）
    → payment-svc：更新支付状态（AT 模式自动回滚）
    → stock-svc：冻结库存（AT 模式自动回滚）
  → Seata TC 提交/回滚
```

#### 5.1.2 最终一致性场景（Saga 补偿 + 消息队列）

适用：下单后的积分发放、优惠券核销、发货通知等异步链路

```
CreateOrder（本地事务）
  → 发送 RocketMQ 延迟消息（15分钟）
      → 检查订单状态
        → 若仍待付款：关闭订单，释放库存
        → 若已付款：ACK，不处理
  → 支付成功事件（RocketMQ）
      → 触发：积分发放、优惠券核销、发货通知
      → 每个步骤均有补偿接口（可重试）
```

#### 5.1.3 事务消息实现

```go
// 使用 RocketMQ 事务消息，确保本地事务和消息发送原子性
func CreateOrderWithTx(ctx context.Context, req *OrderReq) error {
    // 1. 执行本地事务（创建订单 + 库存预占）
    _, err := db.ExecContext(ctx, "BEGIN")
    orderId, err := createOrderAndFreezeStock(ctx, req)
    if err != nil {
        db.ExecContext(ctx, "ROLLBACK")
        return err
    }
    // 2. 发送半消息
    msg := rocketmq.NewMessage("order_pay_topic", req.OrderNo)
    msg.WithProperties(map[string]string{"order_id": orderId})
    _, err = mq.SendHalfMessage(ctx, msg)
    if err != nil {
        db.ExecContext(ctx, "ROLLBACK")
        return err
    }
    // 3. 提交本地事务
    db.ExecContext(ctx, "COMMIT")
    return nil
}
```

### 5.2 缓存架构：多级缓存 + 缓存失效策略

#### 5.2.1 缓存层级

| 层级 | 技术 | 用途 | TTL |
|------|------|------|-----|
| L1 本地缓存 | Caffeine / Go sync.Map | 热点 SKU 信息、类目树 | 30s~5min |
| L2 分布式缓存 | Redis Cluster | 会话、热度数据、分布式锁 | 5min~1h |
| L3 数据库 | MySQL/TiDB | 最终数据源 | 持久 |

#### 5.2.2 缓存更新策略

**Cache-Aside（旁路缓存）**：

```
读：cache hit → 直接返回；miss → 读 DB → 写 cache → 返回
写：更新 DB → 删除 cache（而非更新）→ 异步预热热点数据
```

**缓存击穿**：使用 Redis SETNX + 分布式锁，单个 key 重建时加锁，TTL 延长一倍。

**缓存雪崩**：热点数据 TTL 随机打散（±10%）；Redis Cluster 高可用；后端服务熔断（Go Breaker）。

#### 5.2.3 热点数据缓存方案

```go
// 商品详情页缓存 key 设计
cacheKey := fmt.Sprintf("product:sku:%d", skuId)
cached, err := redis.Get(ctx, cacheKey).Result()
if err == nil {
    return unmarshalProduct(cached)
}
// Redis miss，使用分布式锁重建
lockKey := fmt.Sprintf("lock:product:sku:%d", skuId)
locked, err := redis.SetNX(ctx, lockKey, "1", 10*time.Second).Result()
if locked {
    defer redis.Del(ctx, lockKey)
    data := fetchFromDB(skuId)
    redis.Set(ctx, cacheKey, marshalProduct(data), 5*time.Minute)
    return data
}
// 未获锁，降级到 DB
return fetchFromDB(skuId)
```

#### 5.2.4 大促热点应对

- **库存数据**：库存不缓存，全量读 DB + Redis 分布式锁（SETNX + Lua 脚本原子扣减）
- **活动页面**：静态化 CDN + 预热，提前 1 小时刷新
- **限购防刷**：Redis INCR 原子计数，单用户频次控制

### 5.3 消息队列架构：RocketMQ 5.x

#### 5.3.1 Topic 规划

| Topic | 用途 | 顺序性 | 持久化 |
|-------|------|-------|--------|
| order_create | 新订单创建事件 | 同一订单顺序 | 是 |
| order_pay_success | 支付成功事件 | 同一订单顺序 | 是 |
| order_delivery | 发货事件 | 同一订单顺序 | 是 |
| stock_change | 库存变动事件 | 无 | 是 |
| points_award | 积分发放事件 | 用户维度有序 | 是 |
| coupon_use | 优惠券核销事件 | 无 | 是 |
| sms_notify | 短信通知 | 无 | 是 |
| aftersale_create | 售后创建事件 | 同一售后单有序 | 是 |

#### 5.3.2 消费组规划

| ConsumerGroup | 订阅 Topic | 用途 |
|--------------|------------|------|
| order-consumer | order_create, order_pay_success | 触发库存确认、积分、通知 |
| marketing-consumer | order_pay_success | 优惠券核销 |
| member-consumer | order_pay_success | 会员成长值 |
| notify-consumer | * | 短信/推送/站内信 |
| settlement-consumer | order_delivery | 结算触发 |

#### 5.3.3 消息可靠性保障

- **生产端**：同步发送 + 失败重试（最多 3 次）；使用事务消息保证本地事务和消息发送原子性
- **消费端**：手动 ACK，逻辑处理成功才 ACK；失败进入重试队列（delay 级别：1s/5s/30s/1min/10min）
- **Broker**：主从同步刷盘（ASYNC_FLUSH）；多副本（Sync Replication）
- **死信队列**：消费失败超过最大次数进入 DLQ，人工处理

#### 5.3.4 消息幂等处理

```go
// 每条消息带唯一消息 ID，消费前检查是否已处理
func ConsumeOrderPaid(msg *OrderPaidMsg) error {
    // 幂等检查：Redis SETNX
    key := fmt.Sprintf("consumed:order_pay:%s", msg.TransNo)
    existed, err := redis.SetNX(ctx, key, "1", 7*24*time.Hour).Result()
    if err != nil {
        return err
    }
    if !existed {
        return nil // 已处理，直接返回
    }
    return processOrderPaid(msg)
}
```

---

## 6. 高可用与可观测性

### 6.1 高可用设计

| 策略 | 实现 |
|------|------|
| 服务无状态 | 所有服务无本地状态，Session 入 Redis，用户数据入 DB |
| 熔断降级 | Go Breaker（Sentinel SDK），接口级熔断，核心链路（下单/支付）不允许降级 |
| 限流 | Kong 网关限流；Sentinel 的匀速排队 + 冷启动 |
| 超时控制 | HTTP/gRPC 默认超时 5s，核心支付链路超时 10s |
| 重试 | 仅 GET/幂等写操作重试，指数退避（1s/2s/4s），最多 3 次 |
| 灰度发布 | Kubernetes + Argo Rollouts，流量逐步放量（1%/5%/10%/100%） |
| 多活 | 同城双活 + 异地灾备；Redis Cluster 主从自动切换；MySQL MGR |

### 6.2 可观测性体系

#### 6.2.1 监控维度

| 维度 | 工具 | 指标示例 |
|------|------|---------|
| 基础设施 | Prometheus + Grafana | CPU/内存/磁盘/网络/QPS |
| 应用性能 | SkyWalking / Jaeger | P99/P999 延迟、错误率 |
| 业务指标 | 自定义 Metrics | GMV/UV/转化率/DAU |
| 日志 | ELK Stack | 结构化日志 → ES → Kibana |
| 告警 | AlertManager | PagerDuty/钉钉/短信 |

#### 6.2.2 全链路追踪

```
请求进入网关 → 分配 traceId → 透传各服务（HTTP Header / gRPC Metadata）
  → order-svc（span: createOrder）
    → stock-svc（span: freezeStock）[子调用]
    → payment-svc（span: createPayment）[子调用]
      → 微信支付回调（外部 span）
```

#### 6.2.3 健康检查

- **Readiness**：服务是否可接收流量（依赖的 DB/Redis/RocketMQ 是否就绪）
- **Liveness**：服务是否存活（进程 PID 存在、内存不超限）
- 两类 Probe 分离，避免滚动发布时误杀

---

## 7. 部署与交付

### 7.1 Kubernetes 部署架构

```
┌─────────────────────────────────────────────────┐
│              Kubernetes Cluster                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │  prod    │  │ staging  │  │   dev    │      │
│  │  Namespace│  │ Namespace│  │ Namespace│      │
│  └──────────┘  └──────────┘  └──────────┘      │
└─────────────────────────────────────────────────┘
```

- 应用容器化（Docker），Helm Chart 打包
- ConfigMap 管理环境配置，Secret 管理密钥（加密存储）
- HPA 自动弹性伸缩（CPU/内存/QPS 指标）
- PodDisruptionBudget 防止可用性下降

### 7.2 交付流程

```
代码合并 → 单元测试 → 构建镜像 → 推送 Harbor
  → ArgoCD 监听 GitOps 仓库 → 自动部署 Staging
  → 人工验证 → 审批 → 灰度发布生产（1%/5%/10%/100%）
```

### 7.3 环境规划

| 环境 | 用途 | 数据 |
|------|------|------|
| dev | 开发联调 | 脱敏样本数据 |
| test | 集成测试 | 脱敏生产规模数据 |
| staging | 预发布 | 准生产数据 |
| prod | 正式环境 | 生产数据 |

---

## 8. 关键技术决策记录

| 序号 | 决策项 | 选型 | 备选方案 | 决策理由 |
|------|--------|------|---------|---------|
| 1 | 服务语言 | Go（核心链路）+ Python（营销/报表） | 全部 Java / 全部 Go | 性能与业务灵活性的平衡 |
| 2 | 数据库 | MySQL + TiDB | 纯 MySQL / 纯 ShardingSphere | TiDB 处理海量订单分库分表，MySQL 处理事务性数据 |
| 3 | 缓存 | Redis Cluster | Codis / Memcached | Redis 功能丰富（Lua 脚本/Stream/PubSub），生态成熟 |
| 4 | 消息队列 | RocketMQ 5.x | Kafka / RabbitMQ | 事务消息支持好，延迟消息，适合电商场景 |
| 5 | 搜索引擎 | Elasticsearch 8.x | OpenSearch / Meilisearch | 商品检索 + 搜索广告 + 关键词竞价，功能完整 |
| 6 | 分布式事务 | Seata AT + Saga | XA 两阶段提交 / 纯 MQ 补偿 | AT 自动补偿，开发效率高；支付强一致用 AT；其余用 Saga |
| 7 | API 网关 | Kong / APISIX | Spring Cloud Gateway / Envoy | 开源高性能，插件生态丰富，支持 K8s Ingress |
| 8 | 配置中心 | Nacos | Apollo / Consul | 服务注册发现 + 配置管理一体化 |

---

## 9. 附录：产出物索引

| 产出物 | 文件名 | 位置 |
|--------|--------|------|
| 本文档 | 系统架构设计与技术方案.md | 本文件 |
| PRD | PRD-货架式全类目电商平台.md | ../p-monpmx6jfkmiyi-worker1/ |
| 接口规范 | API-接口规范.md | 待后续（由 Alice 产出） |
| 数据库详设 | DB-详细设计.md | 待后续 |
| 部署方案 | DEPLOY-部署方案.md | 待后续（由 Ella 产出） |
